# ScoutSystem 旅團轉駁器 (Troop Router)

這是旅團系統的「中央市集」。

## 部署方式
1. 將此資料夾上傳至 GitHub Repo。
2. 開啟 GitHub Pages 或部署至 Vercel。
3. 部署後的 `registry.json` URL 將被旅團 Portal 引用。

## 如何新增元件？
直接編輯 `registry.json`，在 `plugins` 陣列中增加新項。
